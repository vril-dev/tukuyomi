# Vhost Sample Data Layout

`data/vhosts/` keeps operator-facing sample docroots for static and PHP-backed vhost previews.

- `samples/`
  - reusable document roots for preview, smoke, and operator validation

These assets intentionally use repository-relative paths only.
