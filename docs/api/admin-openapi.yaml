openapi: 3.0.3
info:
  title: tukuyomi Admin API
  version: 1.0.0
  description: |
    OpenAPI document for the admin endpoints exposed by tukuyomi.

    The default admin API base path is `/tukuyomi-api`. It can be changed by configuration,
    so treat these paths as the default contract.

    Browser-driven admin traffic can exchange a server-side admin API key for a
    same-origin session by calling `/tukuyomi-api/auth/login`. After login,
    read-only admin calls can use the `tukuyomi_admin_session` cookie. State-changing
    admin calls can use either `X-API-Key` or the same-origin session flow, but
    session-authenticated writes must also send `X-CSRF-Token` matching the
    `tukuyomi_admin_csrf` cookie.
servers:
  - url: /
security:
  - ApiKeyAuth: []
  - AdminSessionCookie: []
tags:
  - name: auth
  - name: status
  - name: logs
  - name: waf
  - name: crs
  - name: bypass
  - name: cache
  - name: country-block
  - name: rate-limit
  - name: notifications
  - name: ip-reputation
  - name: bot-defense
  - name: semantic
  - name: verify
  - name: fp-tuner
paths:
  /tukuyomi-api/auth/session:
    get:
      tags: [auth]
      security: []
      summary: Get the current browser admin session state
      description: |
        This endpoint is intentionally callable without `X-API-Key`.

        When the request carries a valid `tukuyomi_admin_session` cookie, the
        response reports the active session and may refresh the
        `tukuyomi_admin_csrf` cookie. Otherwise it returns `authenticated: false`.
      responses:
        '200':
          description: Session status for the current browser context
          headers:
            Set-Cookie:
              description: |
                May refresh the `tukuyomi_admin_csrf` cookie for an active session.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSessionResponse'

  /tukuyomi-api/auth/login:
    post:
      tags: [auth]
      security: []
      summary: Exchange an admin API key for a same-origin admin session
      description: |
        Supply `api_key` in the JSON body. On success the server sets both
        `tukuyomi_admin_session` and `tukuyomi_admin_csrf` cookies.

        This endpoint is intended for browser login flows. Non-browser
        automation can keep using `X-API-Key` directly on admin endpoints.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminLoginRequest'
      responses:
        '200':
          description: Admin session issued successfully
          headers:
            Set-Cookie:
              description: |
                Sets the `tukuyomi_admin_session` and `tukuyomi_admin_csrf` cookies.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminLoginResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/auth/logout:
    post:
      tags: [auth]
      security: []
      summary: Clear the current browser admin session cookies
      description: |
        This endpoint always clears admin auth cookies.

        If the request carries a valid `tukuyomi_admin_session` cookie, the
        caller must also send `X-CSRF-Token` matching the
        `tukuyomi_admin_csrf` cookie. Otherwise the server returns `403`.
      responses:
        '200':
          description: Admin session cookies cleared
          headers:
            Set-Cookie:
              description: |
                Clears the `tukuyomi_admin_session` and `tukuyomi_admin_csrf` cookies.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '403':
          $ref: '#/components/responses/Forbidden'

  /tukuyomi-api/status:
    get:
      tags: [status]
      summary: Get runtime status and effective configuration snapshot
      responses:
        '200':
          description: Status snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusResponse'

  /tukuyomi-api/metrics:
    get:
      tags: [status]
      summary: Export Prometheus-style runtime metrics
      responses:
        '200':
          description: Prometheus exposition format
          content:
            text/plain:
              schema:
                type: string

  /tukuyomi-api/verify-manifest:
    get:
      tags: [verify]
      summary: Export a verification manifest scaffold for external WAF test runners
      responses:
        '200':
          description: Verification manifest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerifyManifestResponse'

  /tukuyomi-api/logs/read:
    get:
      tags: [logs]
      summary: Read log entries with cursor-based pagination
      parameters:
        - $ref: '#/components/parameters/WebLogSource'
        - $ref: '#/components/parameters/TailQuery'
        - $ref: '#/components/parameters/DirQuery'
        - $ref: '#/components/parameters/CursorQuery'
        - $ref: '#/components/parameters/CountryQuery'
      responses:
        '200':
          description: Log page
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogsReadResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/logs/stats:
    get:
      tags: [logs]
      summary: Get WAF block summary and hourly series
      parameters:
        - $ref: '#/components/parameters/HoursQuery'
        - $ref: '#/components/parameters/ScanQuery'
      responses:
        '200':
          description: Log statistics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogsStatsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/logs/download:
    get:
      tags: [logs]
      summary: Download matching log lines as gzipped NDJSON
      description: |
        Response body is NDJSON and the implementation sets `Content-Encoding: gzip`.
      parameters:
        - $ref: '#/components/parameters/WebLogSource'
        - $ref: '#/components/parameters/FromQuery'
        - $ref: '#/components/parameters/ToQuery'
        - $ref: '#/components/parameters/CountryQuery'
      responses:
        '200':
          description: Gzipped NDJSON stream
          content:
            application/x-ndjson:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'

  /tukuyomi-api/rules:
    get:
      tags: [waf]
      summary: Get active editable WAF rule files
      responses:
        '200':
          description: Rule file map plus per-file details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RulesFilesResponse'
    put:
      tags: [waf]
      summary: Save one editable WAF rule file and hot-reload
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuleFilePutRequest'
      responses:
        '200':
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/rules:validate:
    post:
      tags: [waf]
      summary: Validate one editable WAF rule file without saving
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuleFilePutRequest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/crs-rule-sets:
    get:
      tags: [crs]
      summary: Get CRS rule-set selection state
      responses:
        '200':
          description: CRS selection snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericConfigResponse'
    put:
      tags: [crs]
      summary: Save CRS rule-set selection and hot-reload
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CRSRuleSetsRequest'
      responses:
        '200':
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/crs-rule-sets:validate:
    post:
      tags: [crs]
      summary: Validate CRS rule-set selection without saving
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CRSRuleSetsRequest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/bypass-rules:
    get:
      tags: [bypass]
      summary: Get bypass rules
      responses:
        '200':
          description: Current bypass rules
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericConfigResponse'
    put:
      tags: [bypass]
      summary: Save bypass rules
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/bypass-rules:validate:
    post:
      tags: [bypass]
      summary: Validate bypass rules without saving
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/cache-rules:
    get:
      tags: [cache]
      summary: Get cache rules
      responses:
        '200':
          description: Current cache rules
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericConfigResponse'
    put:
      tags: [cache]
      summary: Save cache rules
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CacheRulesRequest'
      responses:
        '200':
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/cache-rules:validate:
    post:
      tags: [cache]
      summary: Validate cache rules without saving
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CacheRulesRequest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/country-block-rules:
    get:
      tags: [country-block]
      summary: Get country block rules
      responses:
        '200':
          description: Current country block rules
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericConfigResponse'
    put:
      tags: [country-block]
      summary: Save country block rules
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/country-block-rules:validate:
    post:
      tags: [country-block]
      summary: Validate country block rules without saving
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/rate-limit-rules:
    get:
      tags: [rate-limit]
      summary: Get rate-limit rules
      responses:
        '200':
          description: Current rate-limit rules
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericConfigResponse'
    put:
      tags: [rate-limit]
      summary: Save rate-limit rules
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/rate-limit-rules:validate:
    post:
      tags: [rate-limit]
      summary: Validate rate-limit rules without saving
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/notifications:
    get:
      tags: [notifications]
      summary: Get notification configuration
      responses:
        '200':
          description: Current notification configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericConfigResponse'
    put:
      tags: [notifications]
      summary: Save notification configuration
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/notifications/status:
    get:
      tags: [notifications]
      summary: Get notification runtime status
      responses:
        '200':
          description: Runtime notification status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdditionalPropertiesObject'

  /tukuyomi-api/notifications/validate:
    post:
      tags: [notifications]
      summary: Validate notification configuration without saving
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/notifications/test:
    post:
      tags: [notifications]
      summary: Send a test notification using the current runtime configuration
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationTestRequest'
      responses:
        '200':
          description: Test notification accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '400':
          $ref: '#/components/responses/BadRequest'

  /tukuyomi-api/ip-reputation:
    get:
      tags: [ip-reputation]
      summary: Get IP reputation configuration and runtime status
      responses:
        '200':
          description: IP reputation state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericConfigResponse'
    put:
      tags: [ip-reputation]
      summary: Save IP reputation configuration
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/ip-reputation:validate:
    post:
      tags: [ip-reputation]
      summary: Validate IP reputation configuration without saving
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/bot-defense-rules:
    get:
      tags: [bot-defense]
      summary: Get bot defense configuration
      responses:
        '200':
          description: Current bot defense rules
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericConfigResponse'
    put:
      tags: [bot-defense]
      summary: Save bot defense configuration
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/bot-defense-rules:validate:
    post:
      tags: [bot-defense]
      summary: Validate bot defense configuration without saving
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/semantic-rules:
    get:
      tags: [semantic]
      summary: Get semantic security configuration and runtime stats
      responses:
        '200':
          description: Current semantic configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericConfigResponse'
    put:
      tags: [semantic]
      summary: Save semantic security configuration
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'

  /tukuyomi-api/semantic-rules:validate:
    post:
      tags: [semantic]
      summary: Validate semantic security configuration without saving
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/fp-tuner/propose:
    post:
      tags: [fp-tuner]
      summary: Propose a scoped false-positive tuning rule
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FPTunerProposeRequest'
      responses:
        '200':
          description: Proposal generated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdditionalPropertiesObject'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'

  /tukuyomi-api/fp-tuner/apply:
    post:
      tags: [fp-tuner]
      summary: Validate or apply a scoped false-positive tuning rule
      security:
        - ApiKeyAuth: []
        - AdminSessionCookie: []
          AdminCSRFCookie: []
          AdminCSRFHeader: []
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FPTunerApplyRequest'
      responses:
        '200':
          description: Proposal applied or simulated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdditionalPropertiesObject'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '500':
          $ref: '#/components/responses/InternalError'
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
      description: |
        Header-based admin credential for CLI, automation, and non-browser
        callers.
    AdminSessionCookie:
      type: apiKey
      in: cookie
      name: tukuyomi_admin_session
      description: |
        Same-origin browser session cookie issued by `/tukuyomi-api/auth/login`.
        Read-only admin requests can use this cookie without `X-API-Key`.
    AdminCSRFCookie:
      type: apiKey
      in: cookie
      name: tukuyomi_admin_csrf
      description: |
        CSRF cookie paired with `tukuyomi_admin_session`. State-changing admin
        requests validate this cookie against `X-CSRF-Token`.
    AdminCSRFHeader:
      type: apiKey
      in: header
      name: X-CSRF-Token
      description: |
        Required on state-changing admin requests when authenticating with the
        session-cookie flow.
  parameters:
    IfMatchHeader:
      name: If-Match
      in: header
      required: false
      description: Optimistic concurrency token returned as `etag` from a prior GET.
      schema:
        type: string
    WebLogSource:
      name: src
      in: query
      required: true
      schema:
        type: string
        enum: [waf, accerr, intr]
    TailQuery:
      name: tail
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
    DirQuery:
      name: dir
      in: query
      required: false
      schema:
        type: string
        enum: [prev]
    CursorQuery:
      name: cursor
      in: query
      required: false
      schema:
        type: integer
        format: int64
    CountryQuery:
      name: country
      in: query
      required: false
      schema:
        type: string
    HoursQuery:
      name: hours
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
    ScanQuery:
      name: scan
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
    FromQuery:
      name: from
      in: query
      required: false
      description: RFC3339 timestamp.
      schema:
        type: string
        format: date-time
    ToQuery:
      name: to
      in: query
      required: false
      description: RFC3339 timestamp.
      schema:
        type: string
        format: date-time
  responses:
    BadRequest:
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Unauthorized:
      description: Missing or invalid admin credentials
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: CSRF validation failed or the current action is not allowed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Conflict:
      description: Optimistic concurrency conflict
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ConflictResponse'
    Unprocessable:
      description: Validation failed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ValidationResult'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
  schemas:
    AdditionalPropertiesObject:
      type: object
      additionalProperties: true
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      additionalProperties: true
    ConflictResponse:
      type: object
      properties:
        error:
          type: string
        currentETag:
          type: string
      additionalProperties: true
    ValidationResult:
      type: object
      properties:
        ok:
          type: boolean
        messages:
          type: array
          items:
            type: string
      additionalProperties: true
    UpdateResult:
      type: object
      properties:
        ok:
          type: boolean
        etag:
          type: string
      additionalProperties: true
    OkResponse:
      type: object
      properties:
        ok:
          type: boolean
      required: [ok]
      additionalProperties: true
    AdminLoginRequest:
      type: object
      properties:
        api_key:
          type: string
      required: [api_key]
      additionalProperties: false
    AdminSessionResponse:
      type: object
      properties:
        authenticated:
          type: boolean
        ok:
          type: boolean
        mode:
          type: string
          enum: [none, session, disabled]
        expires_at:
          type: string
          format: date-time
        csrf_cookie_name:
          type: string
        csrf_header_name:
          type: string
        session_cookie:
          type: string
        session_ttl_secs:
          type: integer
        same_origin_only:
          type: boolean
        cookie_secure_now:
          type: boolean
      required: [authenticated, mode, csrf_header_name]
      additionalProperties: true
    AdminLoginResponse:
      allOf:
        - $ref: '#/components/schemas/AdminSessionResponse'
        - type: object
          properties:
            ok:
              type: boolean
          required: [ok]
    StatusResponse:
      type: object
      additionalProperties: true
      description: |
        Runtime status object. Includes top-level fields such as `status`, configured file paths,
        rate-limit state, notification counters, IP reputation state, semantic counters, DB state,
        and the effective admin API base path.
    RulesFilesResponse:
      type: object
      properties:
        rules:
          type: object
          additionalProperties:
            type: string
        files:
          type: array
          items:
            type: object
            properties:
              path:
                type: string
              raw:
                type: string
              etag:
                type: string
              error:
                type: string
            additionalProperties: true
      additionalProperties: true
    GenericConfigResponse:
      type: object
      properties:
        etag:
          type: string
        raw:
          type: string
      additionalProperties: true
    LogsReadResponse:
      type: object
      properties:
        lines:
          type: array
          items:
            type: object
            additionalProperties: true
        next_cursor:
          type: integer
          format: int64
        page_start:
          type: integer
          format: int64
        page_end:
          type: integer
          format: int64
        has_more:
          type: boolean
        has_prev:
          type: boolean
        has_next:
          type: boolean
      additionalProperties: true
    LogsStatsResponse:
      type: object
      additionalProperties: true
      description: |
        Includes `generated_at`, `scanned_lines`, `range_hours`, optional scan window timestamps,
        and a `waf_block` summary object with counters, top buckets, and hourly series.
    VerifyManifestResponse:
      type: object
      description: |
        Manifest for external verification tools. Includes WAF, security, bot-defense,
        canonical routes, routing summaries, bypass/cache metadata, and derived
        `normal_flows` / `attack_expectations` with route and execution hints.
      properties:
        schema_version:
          type: string
        product:
          type: string
        generated_at:
          type: string
          format: date-time
        base_url:
          type: string
        api_base_path:
          type: string
        config_hash:
          type: string
        component_hashes:
          $ref: '#/components/schemas/VerifyManifestComponentHashes'
        waf:
          type: object
          additionalProperties: true
        security:
          type: object
          additionalProperties: true
        country_block:
          type: object
          additionalProperties: true
        rate_limit:
          type: object
          additionalProperties: true
        ip_reputation:
          type: object
          additionalProperties: true
        semantic:
          type: object
          additionalProperties: true
        bot_defense:
          type: object
          additionalProperties: true
        routes:
          type: array
          items:
            $ref: '#/components/schemas/VerifyManifestRoute'
        routing:
          type: object
          additionalProperties: true
        bypass_rules:
          type: array
          items:
            type: object
            additionalProperties: true
        cache_rules:
          type: array
          items:
            type: object
            additionalProperties: true
        normal_flows:
          type: array
          items:
            $ref: '#/components/schemas/VerifyManifestExpectedFlow'
        attack_expectations:
          type: array
          items:
            $ref: '#/components/schemas/VerifyManifestAttackExpectation'
      additionalProperties: true
    VerifyManifestComponentHashes:
      type: object
      properties:
        waf: { type: string }
        security: { type: string }
        country_block: { type: string }
        rate_limit: { type: string }
        ip_reputation: { type: string }
        semantic: { type: string }
        bot_defense: { type: string }
        routes: { type: string }
        routing: { type: string }
        bypass_rules: { type: string }
        cache_rules: { type: string }
        normal_flows: { type: string }
        attack_expectations: { type: string }
      additionalProperties: true
    VerifyManifestRoute:
      type: object
      properties:
        name:
          type: string
        route_hash:
          type: string
        host:
          type: string
        route_mode:
          type: string
        match_type:
          type: string
        match_value:
          type: string
        path_prefix:
          type: string
        methods:
          type: array
          items:
            type: string
        waf_mode:
          type: string
        bypass:
          type: boolean
        bypass_modes:
          type: array
          items:
            type: string
        cache:
          type: object
          additionalProperties: true
        rate_limit:
          type: object
          additionalProperties: true
        bot_defense:
          type: object
          additionalProperties: true
        security:
          type: object
          additionalProperties: true
      additionalProperties: true
    VerifyManifestScenarioRequest:
      type: object
      properties:
        query:
          type: object
          additionalProperties:
            type: string
        headers:
          type: object
          additionalProperties:
            type: string
        cookies:
          type: object
          additionalProperties:
            type: string
        form:
          type: object
          additionalProperties:
            type: string
        json:
          type: object
          additionalProperties:
            type: string
      additionalProperties: false
    VerifyManifestScenarioAssertions:
      type: object
      properties:
        status_codes:
          type: array
          items:
            type: integer
        final_url_prefixes:
          type: array
          items:
            type: string
        max_console_errors:
          type: integer
        max_network_errors:
          type: integer
      additionalProperties: false
    VerifyManifestExpectedFlow:
      type: object
      properties:
        name:
          type: string
        path:
          type: string
        method:
          type: string
        expect:
          type: string
        route:
          type: string
        route_hash:
          type: string
        requires_auth:
          type: boolean
        execution_mode:
          type: string
        request:
          $ref: '#/components/schemas/VerifyManifestScenarioRequest'
        assertions:
          $ref: '#/components/schemas/VerifyManifestScenarioAssertions'
      additionalProperties: true
    VerifyManifestAttackExpectation:
      type: object
      properties:
        name:
          type: string
        route:
          type: string
        route_hash:
          type: string
        path:
          type: string
        method:
          type: string
        carrier:
          type: string
        param:
          type: string
        payload_type:
          type: string
        payload_value:
          type: string
        technique:
          type: string
        expect:
          type: string
        requires_auth:
          type: boolean
        use_browser:
          type: boolean
        request:
          $ref: '#/components/schemas/VerifyManifestScenarioRequest'
        assertions:
          $ref: '#/components/schemas/VerifyManifestScenarioAssertions'
      additionalProperties: true
    RawRequest:
      type: object
      required: [raw]
      properties:
        raw:
          type: string
    RuleFilePutRequest:
      type: object
      required: [path, raw]
      properties:
        path:
          type: string
        raw:
          type: string
    CacheRulesRequest:
      type: object
      properties:
        rawMode:
          type: boolean
        raw:
          type: string
        rules:
          type: array
          items:
            type: object
            additionalProperties: true
      additionalProperties: false
    CRSRuleSetsRequest:
      type: object
      properties:
        enabled:
          type: array
          items:
            type: string
      additionalProperties: false
    NotificationTestRequest:
      type: object
      properties:
        note:
          type: string
    FPTunerEventInput:
      type: object
      properties:
        event_id:
          type: string
        event_type:
          type: string
        observed_at:
          type: string
          format: date-time
        method:
          type: string
        path:
          type: string
        policy:
          type: string
        rule_id:
          type: integer
        status:
          type: integer
        score:
          type: integer
        reason:
          type: string
        matched_variable:
          type: string
        matched_value:
          type: string
      additionalProperties: false
    FPTunerProposal:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        summary:
          type: string
        reason:
          type: string
        confidence:
          type: number
        target_path:
          type: string
        rule_line:
          type: string
      additionalProperties: false
    FPTunerProposeRequest:
      type: object
      properties:
        event:
          $ref: '#/components/schemas/FPTunerEventInput'
        events:
          type: array
          items:
            $ref: '#/components/schemas/FPTunerEventInput'
        target_path:
          type: string
      additionalProperties: false
    FPTunerApplyRequest:
      type: object
      required: [proposal]
      properties:
        proposal:
          $ref: '#/components/schemas/FPTunerProposal'
        simulate:
          type: boolean
        approval_token:
          type: string
      additionalProperties: false
