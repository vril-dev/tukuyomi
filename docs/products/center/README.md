# tukuyomi-center

`tukuyomi-center` is the center-side management product in the Tukuyomi family.

Its future publication path is separate from the current WAF / reverse proxy
work in `tukuyomi`. Archived center release assets remain in `tukuyomi-releases`.

See [Product Comparison](../../product-comparison.md) for the current product
boundary.
