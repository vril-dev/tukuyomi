# tukuyomi-edge

`tukuyomi-edge` は Tukuyomi ファミリーの edge/device 向け runtime です。

現在の公開 `tukuyomi` リポジトリは WAF / reverse proxy / API gateway を主対象にします。
edge 統合は別途扱い、過去の edge release assets は `tukuyomi-releases` に archive として残します。

現行の製品境界は [製品比較](../../product-comparison.ja.md) を参照してください。
