# Placeholder broker settings for a daemon Runtime App.
# The sample script does not bind this address; replace the script with a real
# broker wrapper before using the value for traffic.
BROKER_LISTEN_ADDR=127.0.0.1:1883
BROKER_HEARTBEAT_SECONDS=30
